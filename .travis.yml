sudo: required
services:
  - docker

before_install:
  - docker build -t jarekjaro/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run jarekjaro/react-test npm test -- --coverage

after_success:
  - docker build -t jarekjaro/multi-client ./client
  - docker build -t jarekjaro/multi-nginx ./nginx
  - docker build -t jarekjaro/multi-server ./server
  - docker build -t jarekjaro/multi-worker ./worker
  # Log in to docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push jarekjaro/multi-client
  - docker push jarekjaro/multi-nginx
  - docker push jarekjaro/multi-server
  - docker push jarekjaro/multi-worker